# Deploy script

# Build directory
weblog build . --style citizen --sitename "The Curious Citizen" --icon --analytics analytics --menu menu

# Stage all changes
git add .

# Commit to repository
git commit

# Push to remote
git push origin master